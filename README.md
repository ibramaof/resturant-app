# Restaurant App

A simple, responsive web-based restaurant application built with **HTML**, **CSS**, and **JavaScript**. This project displays a menu of items and allows users to browse food options in a restaurant UI.

## Features

- Displays a list/menu of restaurant items
- Basic UI interactions (e.g., selecting items, browsing)
- Local data source for menu items (`data.js`)
